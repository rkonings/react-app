mutation UPDATE_ACTIVITY($activity: UpdateActivityInput) {
    updateActivity(activity: $activity) {
        _id
        title
        notes
        type
        creationDate
        client
        dueDate
    }
}
