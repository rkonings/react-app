mutation update($user: InputUser) {
    updateUser(user: $user) {
        _id
        email
        firstName
        lastName
        settings {
            language
            dateFormat
            pushNotifications
            unscribeEmailLink
            signature
        }
    }
}
