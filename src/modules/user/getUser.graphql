query {
    user {
        email
        firstName
        lastName
        settings {
            language
            dateFormat
            pushNotifications
            unscribeEmailLink
            signature
        }
    }
}
